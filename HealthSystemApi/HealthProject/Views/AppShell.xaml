<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="HealthProject.Views.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:HealthProject.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="HealthProject">

    <!-- Home Section -->
    <FlyoutItem Title="Начало">
        <ShellContent
            Title="Начало"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Клиники">
        <ShellContent
            Title="Начало"
            ContentTemplate="{DataTemplate local:HomePage}"
            Route="HomePage"/>
    </FlyoutItem>

    <FlyoutItem Title="Здравни проблеми">
        <ShellContent
            Title="Здравни проблеми"
            ContentTemplate="{DataTemplate local:HealthIssuesPage}"
            Route="HealthIssuesPage"/>
    </FlyoutItem>
    
    <FlyoutItem Title="Симптоми">
        <ShellContent
            Title="Здравни проблеми"
            ContentTemplate="{DataTemplate local:ProblemsViewPage}"
            Route="ProblemsViewPage"/>
    </FlyoutItem>

    <FlyoutItem Title="Автентикация">
        <Tab>
            <ShellContent
                Title="Регистрация"
                ContentTemplate="{DataTemplate local:RegisterPage}"
                Route="RegisterPage"/>
            <ShellContent
                Title="Вписване"
                ContentTemplate="{DataTemplate local:LoginPage}"
                Route="LoginPage"/>
        </Tab>
    </FlyoutItem>

    <!-- Uncomment or remove these sections if you need them visible again -->
    <!--
    <FlyoutItem Title="Hospitals">
        <Tab>
            <ShellContent
                Title="Add Hospital"
                ContentTemplate="{DataTemplate local:AddHospitalPage}"
                Route="AddHospitalPage"/>
            <ShellContent
                Title="Hospital Details"
                ContentTemplate="{DataTemplate local:HospitalDetailsPage}"
                Route="HospitalDetailsPage"/>
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Doctors">
        <Tab>
            <ShellContent
                Title="Add Doctor"
                ContentTemplate="{DataTemplate local:AddDoctorPage}"
                Route="AddDoctorPage"/>
            <ShellContent
                Title="Doctor Details"
                ContentTemplate="{DataTemplate local:DoctorDetailsPage}"
                Route="DoctorDetailsPage"/>
            <ShellContent
                Title="Edit Doctor Info"
                ContentTemplate="{DataTemplate local:EditDoctorInfo}"
                Route="EditDoctorInfo"/>
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Services">
        <Tab>
            <ShellContent
                Title="Add Service"
                ContentTemplate="{DataTemplate local:AddServicePage}"
                Route="AddServicePage"/>
            <ShellContent
                Title="Service Details"
                ContentTemplate="{DataTemplate local:ServiceDetailsPage}"
                Route="ServiceDetailsPage"/>
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Bookings">
        <ShellContent
            Title="Booking"
            ContentTemplate="{DataTemplate local:BookingPage}"
            Route="BookingPage"/>
    </FlyoutItem>
    -->
</Shell>
